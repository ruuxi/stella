{
  "name": "{{name}}",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build",
    "generate": "spacetime generate {{spacetimedbModule}} --lang typescript --out-dir src/bindings --module-path ../../../../spacetimedb --yes"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "spacetimedb": "^2.0.4"
  },
  "devDependencies": {
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "5.2.0",
    "esbuild": "0.27.0",
    "typescript": "~5.9.3",
    "vite": "7.3.1"
  }
}
