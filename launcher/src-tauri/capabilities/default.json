{
  "identifier": "default",
  "description": "Default capabilities for the launcher",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:default"
  ]
}
